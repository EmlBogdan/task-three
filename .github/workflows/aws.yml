# This workflow will build and push a new container image to Amazon ECR,
# and then will deploy a new task definition to Amazon ECS, when there is a push to the "main" branch.
#
# To use this workflow, you will need to complete the following set-up steps:
#
# 1. Create an ECR repository to store your images.
#    For example: `aws ecr create-repository --repository-name my-ecr-repo --region us-east-2`.
#    Replace the value of the `ECR_REPOSITORY` environment variable in the workflow below with your repository's name.
#    Replace the value of the `AWS_REGION` environment variable in the workflow below with your repository's region.
#
# 2. Create an ECS task definition, an ECS cluster, and an ECS service.
#    For example, follow the Getting Started guide on the ECS console:
#      https://us-east-2.console.aws.amazon.com/ecs/home?region=us-east-2#/firstRun
#    Replace the value of the `ECS_SERVICE` environment variable in the workflow below with the name you set for the Amazon ECS service.
#    Replace the value of the `ECS_CLUSTER` environment variable in the workflow below with the name you set for the cluster.
#
# 3. Store your ECS task definition as a JSON file in your repository.
#    The format should follow the output of `aws ecs register-task-definition --generate-cli-skeleton`.
#    Replace the value of the `ECS_TASK_DEFINITION` environment variable in the workflow below with the path to the JSON file.
#    Replace the value of the `CONTAINER_NAME` environment variable in the workflow below with the name of the container
#    in the `containerDefinitions` section of the task definition.
#
# 4. Store an IAM user access key in GitHub Actions secrets named `AWS_ACCESS_KEY_ID` and `AWS_SECRET_ACCESS_KEY`.
#    See the documentation for each action used below for the recommended IAM policies for this IAM user,
#    and best practices on handling the access key credentials.

name: Deploy to Amazon ECS

on:
  push:
    branches: [ "main" ]

jobs:
  build-ollama:
    name: Build ollama
    runs-on: ubuntu-latest
    outputs:
      is_changed: ${{ steps.changes.outputs.src }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - uses: dorny/paths-filter@v4
        id: changes
        with:
          filters: |
            src:
              - 'images/ollama/**'

      - name: Build ollama
        if: steps.changes.outputs.src == 'true'
        id: build-ollama-image
        run: |
          docker build -t ollama:latest -f images/ollama/ollama.Dockerfile .  
          docker save -o ollama-image.tar ollama:latest
      
      - name: Upload ollama image
        if: steps.changes.outputs.src == 'true'
        id: upload-ollama-image
        uses: actions/upload-artifact@v4
        with: 
          name: ollama-docker-image
          path: ollama-image.tar
          retention-days: 1

  deploy-ollama:
    name: Deploy ollama
    permissions:
      id-token: write
      contents: read
    runs-on: ubuntu-latest
    needs: build-ollama
    if: needs.build-ollama.outputs.is_changed == 'true'
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download image artifact
        uses: actions/download-artifact@v4
        with: 
          name: ollama-docker-image

      - name: Load Docker image
        run: docker load -i ollama-image.tar

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v6.2.3
        with:
          role-to-assume: ${{secrets.AWS_IAM_ROLE}}
          aws-region: us-east-1
          role-session-name: GHActionsSession

      - name: Login to Amazon ECR
        id: login-ecr
        env:
          ECR_REPOSITORY: 324062893129.dkr.ecr.us-east-1.amazonaws.com/ollama-repository
        run: | 
          aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin $ECR_REPOSITORY

      - name: Tag ollama image 
        id: tag-ollama-image
        env:
          ECR_REPOSITORY: 324062893129.dkr.ecr.us-east-1.amazonaws.com/ollama-repository
          IMAGE_TAG: ollama
        run: |
          docker tag ollama:latest ollama:$IMAGE_TAG
          docker tag ollama:${IMAGE_TAG} $ECR_REPOSITORY:${IMAGE_TAG}

      - name: Push images
        env:
          ECR_REPOSITORY: 324062893129.dkr.ecr.us-east-1.amazonaws.com/ollama-repository
        id: push-images
        run: |
          docker push ${ECR_REPOSITORY}:ollama

      - name: Download task definition
        run: |
          aws ecs describe-task-definition --task-definition ollama --query taskDefinition > task-definition.json 
        
      - name: Fill in the new image ID in the Amazon ECS task definition
        id: task-def
        uses: aws-actions/amazon-ecs-render-task-definition@v1
        env:
          ECR_REPOSITORY: 324062893129.dkr.ecr.us-east-1.amazonaws.com/ollama-repository
        with:
          task-definition: task-definition.json
          container-name: ollama-container
          image: "${{env.ECR_REPOSITORY}}:ollama"
      

      - name: Deploy Amazon ECS task definition
        uses: aws-actions/amazon-ecs-deploy-task-definition@v2
        with:
          task-definition: ${{ steps.task-def.outputs.task-definition }}
          service: ollama
          cluster: ollama-cluster
          wait-for-service-stability: true











    # - name: Pull dashboards
    #   id: pull-dashboards-for-prov
    #   env: 
    #     GITHUB_REPO: https://github.com/EmlBogdan/dashboards
    #   run: |
    #     git clone ${GITHUB_REPO} dashboards
      
    # - name: Pull alerts
    #   id: pull-alerts-for-prov
    #   env: 
    #     GITHUB_REPO: https://github.com/EmlBogdan/alert_rules
    #   run: |
    #     git clone ${GITHUB_REPO} rules  
      


    

    # - name: Build grafana image 
    #   id: build-grafana-image
    #   env:
    #     ECR_REPOSITORY: 324062893129.dkr.ecr.us-east-1.amazonaws.com/ollama-repository
    #   run: |
    #     docker pull ghcr.io/open-webui/open-webui:main
    
    # - name: Tag grafana image 
    #   id: tag-grafana-image
    #   env:
    #     ECR_REPOSITORY: 324062893129.dkr.ecr.us-east-1.amazonaws.com/ollama-repository
    #     IMAGE_TAG: webui-base-image
    #   run: |
    #     docker tag ghcr.io/open-webui/open-webui:main ghcr.io/open-webui/open-webui:$IMAGE_TAG
    #     docker tag ghcr.io/open-webui/open-webui:${IMAGE_TAG} $ECR_REPOSITORY:${IMAGE_TAG}


    # - name: Push images
    #   env:
    #     ECR_REPOSITORY: 324062893129.dkr.ecr.us-east-1.amazonaws.com/ollama-repository
    #   id: push-images
    #   run: |
    #     docker push ${ECR_REPOSITORY}:webui-base-image
    
    

    

